Current progress:

✅ Ingestion
⬜ Validation
⬜ Preprocessing
⬜ Feature Engineering

1) CREATE data_preprocessing.py inside ml/src

    🔴 Important Architecture Decision
    We should NOT pass pandas dataframe between Airflow tasks directly.
    Instead:
    save outputs to files
    This is production style.

    🔴 So preprocessing task will:
    INPUT:
    /opt/airflow/data/housing.csv
    OUTPUT:
    /opt/airflow/data/processed/
    with:

    X_train.csv
    X_test.csv
    y_train.csv
    y_test.csv


2) Create: data/processed/
    data/
    ├── housing.csv
    └── processed/

3) Data Validation

    Now we add:
    validation layer
    before preprocessing.

    🔴 Why validation matters

    Without validation:

    wrong CSV format
    missing target column
    empty dataset
    schema mismatch

    can crash pipeline later.

4) REWRITTING GENRIC CODE FOR ALL FIEL:
    - injsution handels -> json, csv
    - data validation -> handels for all datasets not just the one we use
    - data preprocessing -> handel all types of files to preprocess

Current status:

✅ Data Ingestion
✅ Data Validation
✅ Data Preprocessing
⬜ Feature Engineering

5) create feature_engineering.py and data_splitting.py

Proper ML order is usually:

1. ingest
2. validate
3. clean data
4. fit feature engineering on train
5. split train/test
6. save artifacts

